apiVersion: v1
kind: Service
metadata:
  name: okps-prometheus
  namespace: okps
  labels:
    app.kubernetes.io/name: okps-prometheus
    app.kubernetes.io/component: monitoring
    app.kubernetes.io/part-of: okps-pipeline
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: okps-prometheus
  ports:
    - name: http
      protocol: TCP
      port: 9090
      targetPort: http
